{
  "name": "wenku",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node api/server.js",
    "build": "echo \"No build step\"",
    "render-build": "npm ci || true"  // Render si volá buildCommand; ať nepadá
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2"
  }
}
